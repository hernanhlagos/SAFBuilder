<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.hernanhlagos</groupId>
    <artifactId>safbuilder</artifactId>
    <version>2.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SAFBuilder</name>
    <description>Generates DSpace Simple Archive Format packages from a CSV metadata file and content files.</description>
    <url>https://github.com/hernanhlagos/SAFBuilder</url>

    <contributors>
        <contributor>
            <name>Peter Dietz</name>
            <url>https://github.com/peterdietz</url>
        </contributor>
    </contributors>

    <properties>
        <!-- Build -->
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <!-- Core dependencies — all pinned, no open ranges -->
        <commons-cli.version>1.9.0</commons-cli.version>
        <commons-csv.version>1.12.0</commons-csv.version>
        <commons-io.version>2.18.0</commons-io.version>
        <commons-compress.version>1.27.1</commons-compress.version>
        <commons-lang3.version>3.17.0</commons-lang3.version>
        <commons-vfs2.version>2.9.0</commons-vfs2.version>
        <jackson.version>2.18.2</jackson.version>
        <juniversalchardet.version>2.5.0</juniversalchardet.version>
        <httpclient5.version>5.4.1</httpclient5.version>

        <!-- Logging -->
        <slf4j.version>2.0.16</slf4j.version>
        <logback.version>1.5.12</logback.version>

        <!-- Propiedades dinámicas para jpackage (vacío por defecto) -->
        <jpackage.os.args></jpackage.os.args>

        <!-- UI -->
        <flatlaf.version>3.5.4</flatlaf.version>

        <!-- jpackage icon args are OS-specific -->
        <jpackage.icon.args></jpackage.icon.args>
        <jpackage.installer.args></jpackage.installer.args>

        <!-- Test -->
        <junit.version>5.11.3</junit.version>
        <mockito.version>5.14.2</mockito.version>

        <!-- Maven plugins -->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    </properties>

    <dependencies>

        <!-- ===== CLI ===== -->
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>${commons-cli.version}</version>
        </dependency>

        <!-- ===== Core: CSV ===== -->
        <!-- Replaces abandoned net.sourceforge.javacsv:javacsv:2.0 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>${commons-csv.version}</version>
        </dependency>

        <!-- ===== Core: I/O ===== -->
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-compress</artifactId>
            <version>${commons-compress.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3.version}</version>
        </dependency>

        <!-- ===== Core: VFS (for .tar.gz filegroups) ===== -->
        <!-- Replaces abandoned commons-vfs:commons-vfs:1.0 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-vfs2</artifactId>
            <version>${commons-vfs2.version}</version>
        </dependency>

        <!-- ===== Core: Charset detection ===== -->
        <!-- Updated groupId for the maintained fork -->
        <dependency>
            <groupId>com.github.albfernandez</groupId>
            <artifactId>juniversalchardet</artifactId>
            <version>${juniversalchardet.version}</version>
        </dependency>

        <!-- ===== Core: JSON (collection validation) ===== -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- ===== Core: HTTP (collection validation) ===== -->
        <!-- Replaces deprecated org.apache.httpcomponents:httpclient:4.x -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
            <version>${httpclient5.version}</version>
        </dependency>

        <!-- ===== Logging: SLF4J API + Logback ===== -->
        <!-- Replaces all System.out.println calls in the codebase -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- ===== UI: FlatLaf (Swing modern look and feel) ===== -->
        <dependency>
            <groupId>com.formdev</groupId>
            <artifactId>flatlaf</artifactId>
            <version>${flatlaf.version}</version>
        </dependency>

        <!-- ===== Test ===== -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <release>${java.version}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>

            <!-- Surefire: required for JUnit 5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
            </plugin>

            <!-- Shade: executable fat JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${maven-shade-plugin.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <!-- Remove signature files that break shaded JARs -->
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>safbuilder.cli.BatchProcess</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <!-- Configuración específica para Windows: añade accesos directos -->
        <profile>
            <id>os-windows</id>
            <activation>
                <os><family>windows</family></os>
            </activation>
            <properties>
                <jpackage.icon.args>--icon ${project.basedir}/src/main/resources/icons/safbuilder.ico</jpackage.icon.args>
                <jpackage.installer.args>--win-shortcut --win-menu</jpackage.installer.args>
            </properties>
        </profile>
        <profile>
            <id>os-mac</id>
            <activation>
                <os><family>mac</family></os>
            </activation>
            <properties>
                <jpackage.icon.args>--icon ${project.basedir}/src/main/resources/icons/safbuilder.icns</jpackage.icon.args>
            </properties>
        </profile>
        <profile>
            <id>os-linux</id>
            <activation>
                <os><name>Linux</name></os>
            </activation>
            <properties>
                <jpackage.icon.args>--icon ${project.basedir}/src/main/resources/icons/safbuilder.png</jpackage.icon.args>
            </properties>
        </profile>
        <profile>
            <id>native-installer</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <mkdir dir="${project.build.directory}/app-jar" />
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar" 
                                              tofile="${project.build.directory}/app-jar/safbuilder.jar" />
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${java.home}/bin/jpackage</executable>
                                    <commandlineArgs>
                                        --input ${project.build.directory}/app-jar
                                        --dest ${project.build.directory}/installer
                                        --name SAFBuilder
                                        --main-jar safbuilder.jar
                                        --main-class safbuilder.cli.BatchProcess
                                        --app-version 2.0.0
                                        --vendor HernanLagos
                                        --copyright "Copyright 2025"
                                        --description "DSpace SAF Package Builder"
                                        ${jpackage.icon.args}
                                        ${jpackage.installer.args}
                                    </commandlineArgs>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>native-app-image</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <mkdir dir="${project.build.directory}/app-jar" />
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar" 
                                              tofile="${project.build.directory}/app-jar/safbuilder.jar" />
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${java.home}/bin/jpackage</executable>
                                    <commandlineArgs>
                                        --input ${project.build.directory}/app-jar
                                        --dest ${project.build.directory}/installer
                                        --name SAFBuilder
                                        --main-jar safbuilder.jar
                                        --main-class safbuilder.cli.BatchProcess
                                        --type app-image
                                        --app-version 2.0.0
                                        --vendor HernanLagos
                                        --copyright "Copyright 2025"
                                        --description "DSpace SAF Package Builder"
                                        ${jpackage.icon.args}
                                    </commandlineArgs>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
